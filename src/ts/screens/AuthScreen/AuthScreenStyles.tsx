import { Typography, withStyles } from "@material-ui/core";

export const RegisterText = withStyles({
    root: {
      display: "flex",
      fontSize: 13,
      height: 18,
      marginTop: 16,
      color: "#656565",
      fontWeight: 400,
    },
  })(Typography);